## Server

This area is used for server learning. Main content is to cover Windows Server setups, where main focus is hands-on tasks.

Each hands-on lab will have a "README.md" to define specific requirements and notes to execute the task, also a picture will be added to see the topology.
